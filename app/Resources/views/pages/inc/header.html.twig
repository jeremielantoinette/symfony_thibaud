<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>Symfony 3 crud</title>
    <link rel="stylesheet" type="text/css" href="{{asset('css/style.css') }}" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta.3/css/bootstrap.min.css" integrity="sha384-Zug+QiDoJOrZ5t4lssLdxGhVrurbmBWopoEl+M6BdEfwnCJZtKxi1KgxUyJq13dy" crossorigin="anonymous">
</head>
<body>
<div class="container-fluid bg-primary">
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-dark ">
            <a class="navbar-brand" href="{{ absolute_url(asset('')) }}">Symfony</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ absolute_url(asset('')) }}">Home <span class="sr-only">(current)</span></a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item">
                            <a style="color: white; margin-right: 15px; margin-top: 20px;" href="{{ logout_path('main') }}">Se deconnecter</a>
                        </li>
                        <li class="nav-item">
                            <a style="color: white; margin-top: 20px;" href="{{ absolute_url(asset('')) }}app_dev.php/post">Admin</a>
                        </li>

                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ absolute_url(asset('')) }}app_dev.php/login">Se connecter</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ absolute_url(asset('')) }}app_dev.php/register">S'inscrire</a>
                        </li>
                    {% endif %}


                </ul>
            </div>
        </nav>
    </div>

</div>
{% for succes_message in app.session.flashbag.get('message') %}
    <div class="alert alert-success container">{{ succes_message }}</div>
{% endfor %}
</body>